# WattWise — Home Energy Builder & Simulator

## Product vision

เว็บแอปพลิเคชันที่ให้ผู้ใช้ประกอบ “บ้านจำลองด้านพลังงาน” จากฐานข้อมูลเครื่องใช้ไฟฟ้ารุ่นจริง แล้วประเมินการใช้พลังงาน ค่าไฟ โหลดสูงสุด และผลลัพธ์จากการปรับพฤติกรรมหรือเปลี่ยนอุปกรณ์

## Primary user flow

1. ค้นหาเครื่องใช้ไฟฟ้าด้วยหมวดหมู่ ยี่ห้อ หรือรหัสรุ่น
2. ลากการ์ดอุปกรณ์เข้าไปในบ้าน หรือกดปุ่มเพิ่ม
3. ระบุห้อง จำนวนเครื่อง และรูปแบบการใช้งาน
4. ระบบเลือกวิธีคำนวณที่เหมาะกับประเภทอุปกรณ์
5. Dashboard อัปเดต kWh และค่าไฟโดยประมาณทันที
6. ผู้ใช้บันทึกบ้านและสร้าง Scenario เพื่อเปรียบเทียบ

## MVP scope

- Appliance catalog พร้อม search/filter
- Drag-and-drop Home Builder
- เพิ่มอุปกรณ์แบบกำหนดเองเมื่อค้นหารุ่นไม่พบ
- Usage profile: จำนวน ชั่วโมงต่อวัน วันต่อเดือน และช่วงเวลาใช้งาน
- Calculation engine 4 วิธี: watt-hours, per-cycle, annual-energy และ variable-load
- Energy summary รายอุปกรณ์/ห้อง/หมวดหมู่
- อัตราค่าไฟแบบหลายขั้น รวม service charge, Ft และ VAT
- บันทึก Household และอุปกรณ์ลง D1
- แสดงแหล่งข้อมูล วันที่ตรวจสอบ และระดับความเชื่อมั่นของสเปก

## Non-goals for the first version

- ระบบสมาชิกหลายผู้ใช้
- การดึงข้อมูลสินค้าจากทุกยี่ห้อแบบอัตโนมัติ
- การรับข้อมูลมิเตอร์หรือ ESP32 แบบ real-time
- การยืนยันว่าค่าที่คำนวณเท่ากับใบแจ้งค่าไฟจริง

## Data quality rules

- ห้ามแสดงข้อมูลตัวอย่างว่าเป็นข้อมูลที่ตรวจสอบแล้ว
- รุ่นที่เผยแพร่จริงต้องมี `source_url`, `source_name` และ `verified_at`
- ใช้ annual kWh หรือ kWh/cycle จากฉลากก่อน rated watt เมื่อมีข้อมูล
- แสดงผลเป็น “ค่าประมาณ” เสมอ และบอก calculation method
- Tariff ต้องมีช่วงวันที่มีผลและแหล่งอ้างอิง

## Acceptance criteria for the first slice

- ผู้ใช้ค้นหาและกรอง Catalog ได้
- ผู้ใช้ลากหรือกดเพิ่มอุปกรณ์เข้าบ้านได้
- ผู้ใช้แก้จำนวนและชั่วโมงใช้งานได้
- พลังงานรวมและค่าไฟตัวอย่างอัปเดตทันที
- UI ใช้งานได้บน desktop และ mobile
- Production build ผ่านโดยไม่มี TypeScript error

## Next milestones

1. เชื่อม Catalog และ Household กับ D1
2. เพิ่ม modal ตั้งค่าห้องและ usage profile
3. ใส่อัตราค่าไฟที่ตรวจสอบจากแหล่งทางการ
4. เพิ่มกราฟ breakdown และ load profile 24 ชั่วโมง
5. เพิ่ม scenario comparison และคำแนะนำประหยัดไฟ
